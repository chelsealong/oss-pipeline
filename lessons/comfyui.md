- [2026-09-03] #16022 skipped: feature request for a new FILE_3D->MESH loader node, not a bug fix; requires new API design (format support, error semantics) beyond a minimal fix
- [2026-09-04] #16097 skipped: #16097 is a user-support issue (labeled "User Support"), not a code bug. sqlalchemy is already declared in requirements.txt and is only missing because the reporter's Windows portable build's embedded Python wasn't updated after a git pull — the portable dis
- [2026-09-05] #16123 skipped: reporter's own diagnostic shows torch.cuda.get_device_properties().total_memory and torch.cuda.mem_get_info() both misreport VRAM (free > total) on a ROCm 7.13 nightly build; the OutOfMemoryError is raised inside PyTorch's own CUDA/HIP allocator, no ComfyUI code in the path — same class as the AMD/ROCm PyTorch-not-ComfyUI bugs already flagged by the briefing heuristic.
- [2026-09-05] #16123 skipped: #16123 is a PyTorch/ROCm driver bug, not a ComfyUI bug — torch.cuda.get_device_properties().total_memory and torch.cuda.mem_get_info() both report incorrect/inconsistent values (free > total) on this ROCm 7.13 nightly build for the Radeon AI PRO R9700; the a
- [2026-09-05] #16131 skipped: #16131 is a "User Support" labeled issue (single reporter, 0 comments) about a missing/stale frontend asset (vendor-firebase-*.js 404) on their local StabilityMatrix-managed ComfyUI install. The frontend is a separate pip package (comfyui-frontend-package) ser
- [2026-09-05] #16132 skipped: fixed already by open PR #16058 ("Fix inherited ComfyNode schema caches"). #16132 reports that V3 ComfyNode subclasses inherit parent's cached _RETURN_TYPES/_RETURN_NAMES etc. via plain `is None` class-attribute guards in comfy_api/latest/_io.py; #16058 changes exactly those guards to `cls.__dict__.get("_X") is None` for every cached attribute (DESCRIPTION, CATEGORY, RETURN_TYPES, RETURN_NAMES, OUTPUT_IS_LIST, etc.) and says "Fixes #16026", a differently-worded report of the same root cause. Neither #16132 nor #16058 cross-link each other by number, so mechanical linked-PR checks miss this pairing — when a similar-titled open PR touches the same file/mechanism, read its full diff against the issue's own suggested fix before assuming it's unrelated just because the issue numbers don't match.
- [2026-09-05] #16132 skipped: #16132 is already fixed by open PR #16058 ("Fix inherited ComfyNode schema caches"), which changes every `if cls._X is None` guard in comfy_api/latest/_io.py's classproperties and GET_SCHEMA() (including _RETURN_TYPES and _RETURN_NAMES, the exact attributes #1
- [2026-09-07] #16155 skipped: #16155 is model behavior, not a code defect. The MiniMax H3 Ref2VA node produces gibberish/mistimed dialogue when a voice-cloning audio reference is supplied, but the issue thread (5 comments) converges on this being a quirk of the model itself: the audio refe
- [2026-09-08] #16175 skipped: #16175 reports MiniMax-H3 OOM/fragmentation when Model Sparse Attention (sol-attn/SLA, not VSA) is enabled, disappearing with --disable-comfy-compiler. This is the same cluster as #16144 (closed, assignee rattus128), which got a partial fix in PR #16148/a99d1f
- [2026-09-08] #16183 skipped: #16183's traceback shows custom_nodes/ComfyUI-MiniMaxH3-Unified/nodes.py calling MiniMaxH3ReferenceToVideo.execute() directly as a Python call, passing width/height as strings. That custom node is a third-party extension not present in this repo; it bypasses C
- [2026-09-08] #16132 skipped: #16132 is already fixed by open PR #16058 ("Fix inherited ComfyNode schema caches"), re-verified now. #16058 modifies comfy_api/latest/_io.py to convert every `if cls._X is None` cache-sentinel guard (including _RETURN_TYPES/_RETURN_NAMES, the exact attributes
