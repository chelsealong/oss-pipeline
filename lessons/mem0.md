- [2026-09-14] #6828 skipped: Issue #6828 (plugin version drift between marketplace.json and plugin.json manifests) is already resolved on current main. A commenter on the issue noted PR #6829 fixed it. I independently verified: all plugin manifests (integrations/cursor-plugin/.cursor-plug
- [2026-09-15] #7342 skipped: Issue #7342 describes mem0 Cursor plugin 0.2.13 spawning visible Git Bash/mintty windows on Windows because hooks were .sh scripts (e.g. on_file_read_cursor.sh) launched via git-bash.exe for a preToolUse/Read hook, and the script's `INPUT=$(cat)` never got EOF
- [2026-09-17] #7355 skipped: Issue #7355 is real (verified by reading node_modules-equivalent source: openai@4.93.0's Node shim at _shims/node-runtime.mjs always uses `node-fetch` + a hardcoded `agentkeepalive` KeepAliveAgent as the default HTTP(S) agent, never Node's global fetch/undici,
- [2026-09-18] #7302 skipped: implemented the fix myself (thread AbortSignal into buildRecallContext/MemoryLifecycle.recall in integrations/agent-plugin-core/typescript/src/lifecycle.ts, pass it from integrations/deepseek-plugin/src/index.ts, added the issue's regression test), verified it fails without the fix and passes with it, all suites green — but `git push` to `fix/deepseek-plugin-cancel-recall-seen` was rejected: a prior run (2026-09-14) already pushed the identical fix and opened PR #7304, "Closes #7302". A second contributor (L4XB) independently found the same bug and opened PR #7329 the same day. Both PRs were auto-closed only by the PR Gate (issue #7302 still lacks the `accepted` label) — not rejected on merits. Lesson: before implementing, check for existing closed PRs referencing the issue number (`gh search prs "<issue#>" --repo <owner>/<repo>`), not just open PRs/assignees/comments — a gate-closed PR from an earlier run of this same pipeline is easy to miss and duplicating it wastes the whole session. If found, the existing PR already reopens automatically once the issue is labeled `accepted`; do not re-implement or push a second branch.
- [2026-09-18] #7302 skipped: issue #7302 already has our own fix pushed and PR'd; re-filing would violate "do not re-file the same change under a new pull request when one is closed"
