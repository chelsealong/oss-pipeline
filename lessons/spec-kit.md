- [2026-09-03] #4423 skipped: Issue #4423 is stage 3 of a 3-stage migration ("This issue is implemented only after #4421 and #4422 have shipped in earlier minor releases"); #4421 (add extension) and #4422 (deprecate core command) are still open/unshipped, so this issue is explicitly not ac
- [2026-09-03] #4427 skipped: Issue author explicitly claimed the work ("I plan to work on this together with @markuswondrak"), and the issue is a large multi-stage feature request (new `preset update` command with staged/atomic rollback, manifest diffing, catalog provenance, constitution 
- [2026-09-03] #4433 skipped: Windows-only OPENSSL_Applink/DLL-search-order crash in the uv-managed `specify.exe` launcher when another app (Codex Desktop) prepends PATH dirs with an ABI-incompatible libssl/libcrypto DLL. No DLL-loading or launcher code exists anywhere in this repo — the launcher and DLL search path come entirely from uv's tool-install machinery. Any in-repo mitigation (e.g. os.add_dll_directory) would be a guess about uv's Windows venv layout, unverifiable and untestable on Linux CI, and risks being a silent no-op fix.
- [2026-09-03] #4433 skipped: Issue #4433 is a Windows-only OPENSSL_Applink/DLL-search-order crash (specify.exe resolving an ABI-incompatible libssl/libcrypto DLL earlier on PATH from Codex Desktop's Poppler/Git dirs) with no DLL-loading, launcher, or OpenSSL-related code anywhere in the r
- [2026-09-09] #4495 skipped: "First-party bugfix and assess bundles with bundled workflows" is a large multi-part feature request (two new workflow YAMLs, two new bundle definitions, builtin://default catalog wiring, contract tests, docs across three files) agreed in principle in #4461 but with an explicit unresolved design question left for the PR ("hosting of the bundle artifacts... to be decided during implementation"). Not a bug fix, no minimal patch fits; skip rather than partial-implement a feature whose shape is still being decided.
- [2026-09-09] #4495 skipped: Issue #4495 is a large multi-stage feature request (two new bundled workflows: bugfix and assess, two new bundle definitions, catalog.json wiring for a builtin://default catalog, contract tests mirroring test_wheel_bundled_presets.py, and docs updates across b
- [2026-09-10] #4505 skipped: "make catalog add command as UPSERT" is a feature request still labeled needs-triage, zero comments, no maintainer verdict. Author proposes two mutually exclusive behaviors ("add + update OR add + ignore") without picking one, and gives no acceptance criteria. add_source() in src/specify_cli/bundler/commands_impl/catalog_config.py currently raises on id-or-url collision by design; upserting requires deciding what "update" means and how id/url mismatches resolve — a real API decision CONTRIBUTING.md says needs prior maintainer agreement for behavior/argument changes.
- [2026-09-10] #4505 skipped: Issue #4505 is a feature request ("make catalog add command as UPSERT") still labeled needs-triage with no maintainer-agreed design. The author offers two mutually exclusive behaviors ("add + update OR add + ignore") without deciding which, and gives no accept
- [2026-09-11] #4527 skipped: Issue #4527 is a CI/workflow configuration defect entirely contained in .github/workflows/lint.yml (a quoted glob in the markdownlint-cli2 invocation causes it to match 0 files). The only possible fix is editing that workflow file, which falls squarely under t
- [2026-09-12] #4551 skipped: Issue #4551 (preset-provided scripts never resolved) is a genuine gap, but closing it requires real feature work, not a minimal bug fix. VALID_PRESET_TEMPLATE_TYPES already includes "script" as a deliberate, tested type (added intentionally in commit b6b3ec4, 
