- [2026-08-25] #6899 skipped: Not a fixable bug without a maintainer design decision that isn't ours to make.
- [2026-08-30] #6949 skipped: #6949 is a feature request ("Describe the Solution You'd Like") asking for two new BaseMemoryService methods, and the reporter explicitly deferred implementation pending maintainer sign-off on the method names/signatures ("happy to open a PR if you're OK with 
- [2026-08-31] #6960 skipped: issue titled " v2" is a fully empty bug-report template (every section still the placeholder text) with zero actual bug description. Also found a stray "picking this up" comment on the issue from our own account with no branch/PR behind it — an earlier run must have claimed it and then died before doing the work. Lesson: an existing self-authored claiming-comment is not evidence a fix was found; always re-verify against the actual issue body before trusting a prior run's comment.
- [2026-08-31] #6960 skipped: Issue #6960 ("v2") is a completely empty bug report template — every section (Describe the Bug, Steps to Reproduce, Expected/Observed Behavior, Environment, etc.) is still the unfilled placeholder text from the issue template. There is no bug description, re
- [2026-08-31] #6964 skipped: #6964 is a feature request for a brand-new documentation sample (contributing/samples/integrations/model_armor_tool_output/), not a code defect. The linked core-library gap ("core plugin extension") is explicitly deferred by the reporter as a separate future i
- [2026-08-31] #6966 skipped: #6966 is a feature request (not a defect) proposing to add tool-output screening to ModelArmorPlugin. The reporter explicitly presents two competing API designs as "(maintainer choice)" — extending ModelArmorConfig with a new field vs. shipping a separate To
- [2026-09-01] #6974 skipped: Issue #6974 is a feature proposal, not a defect. It explicitly asks maintainers design questions ("Is this in scope? Where should the hook live — in the Tool base class or agent loop? Sync or async verifier?") about adding an optional trust_verifier hook to 
- [2026-09-01] #6979 skipped: Issue #6979 is a feature request ("Expose current callback hook metadata in Context"), not a defect. The reporter explicitly frames the API as undecided ("The exact API is open for discussion"), proposes multiple alternative designs (mutable Context field vs i
- [2026-09-02] #6990 skipped: Issue #6990 (title "Mz") is a completely empty feature-request template — every section (problem description, desired solution, impact, willingness to contribute) is still the unfilled placeholder text. The single comment on the issue reads "Duplicate of ###
- [2026-09-03] #7001 skipped: Issue #7001 is a public pointer to a private Google issuetracker report (issuetracker.google.com/issues/556637640) with no vulnerability details, affected file/code, or reproduction steps in the GitHub issue body itself. Attempted to fetch the linked tracker; 
- [2026-09-04] #7009 skipped: #7009 is a version-constraint question in pyproject.toml (eval extra's google-cloud-aiplatform bound lacks the <2 cap that all/gcp/test have). Two hard constraints rule this out: (1) the lessons file bans touching dependency manifests/lockfiles, and the fix he
- [2026-09-04] #7010 skipped: require_confirmation callable returning a non-bool passes through cast(bool, ...) unchanged at runtime, silently skipping the confirmation gate. Reporter explicitly refused to propose a fix, listing 3 candidate semantics (bool() coercion still wrong, isinstance check breaks existing truthy-string-reason callers, fail-closed-on-falsy-non-bool flips other existing callers' behavior) and said "Which non-bools stay legal is a design decision." Do not implement any of these unilaterally — it's a maintainer call. Also found a stray self-authored "picking this up" comment with no branch/PR behind it (same dead-claim pattern as #6960) — do not trust prior claiming-comments as evidence of completed work.
- [2026-09-04] #7010 skipped: Issue #7010 reports that a require_confirmation callable returning a non-bool (e.g. None) is passed through cast(bool, ...) at runtime, silently bypassing the confirmation gate. The reporter explicitly declines to propose a fix, laying out three candidate beha
- [2026-09-05] #7021 skipped: Issue #7021 (title "syria") is a completely empty bug-report template — every section (Describe the Bug, Steps to Reproduce, Expected/Observed Behavior, Environment Details, Model Information) is still unfilled placeholder text. No bug is described, so there
- [2026-09-08] #7050 skipped: Issue #7050 is a promotional/self-advertising post ("ADK analyzed by Claude and compared with other memory systems") linking to an external blog post and a Discord invite. It contains no bug report, no feature request, and no concrete code change — there is 
- [2026-09-09] #7065 skipped: Issue #7065 is already fixed by open PR #7066 (same author artemkulyk, opened same day), which explicitly says "Closes: #7065" and implements exactly the fix described (reject path params with '.'/'..' segments in RestApiTool._encode_path_param before quote(sa
- [2026-09-11] #7094 skipped: #7094 is a feature request, not a defect. It explicitly builds on the in-progress PR #6592 (AgentDispatcherToolset), proposing an additive on_event callback API. The reporter states "Willingness to contribute: No — happy to share our working approach as a re
- [2026-09-12] #7102 skipped: Already fixed on main. Commit 790cb7c4 ("fix(workflow): drop resume inputs from the resumable node checkpoint", authored 2026-09-01, present since tag v2.9.0, already an ancestor of the current work-base/upstream-main HEAD) removes "resume_inputs" from the inc
- [2026-09-13] #7103 skipped: #7103 is an RFC, not a concrete bug or minimal fix. The reporter proposes a new "provenance/context-boundary" security layer (tagging content sources, exposing that to before_tool_callback/ToolConfirmation) and explicitly asks maintainers to pick a direction b
- [2026-09-14] #7111 skipped: Issue #7111 is a vendor/promotional proposal (MemCode CEO pitching their commercial memory-layer product as an ADK integration), not a defect or concrete PR-able change. It explicitly asks for a maintainer decision/technical call on the extension point. Found 
- [2026-09-16] #7137 skipped: Issue #7137 is a feature request, not a defect. It asks for a brand-new synchronous API on AgentRegistry (a `published_skills.get(name=...)` accessor mirroring the Console/COPYTOREPOTOUSE.txt snippet) to replace the currently-nonexistent surface. The reporter 
- [2026-09-16] #7137 skipped: Issue #7137 is a feature request, not a bug fix. It asks for a brand-new synchronous `AgentRegistry.published_skills.get(name=...)` accessor (plus implied `list`/`search`) so a Google Cloud Console code snippet becomes runnable. No such API exists anywhere in 
- [2026-09-17] #7156 skipped: Issue #7156 has no content — title is literally the GitHub "issue chooser" URL and the body is the entirely unfilled bug-report template (every section still placeholder text: Describe the Bug, Steps to Reproduce, Expected/Observed Behavior, Environment Deta
- [2026-09-18] #7192 skipped: Issue #7192's own body (Willingness to contribute) states the reporter "has the fix ready with unit tests, one commit per item above, and can open the PR once this is triaged," with concrete evidence from running the shared session contract suite against a rea
- [2026-09-19] #7205 skipped: Issue #7205's own body ("Suggested fix") states the reporter (1aifanatic) "have a PR ready with a test" for exactly this fix (skip path-level parameters whose (name, in) an operation already declares when merging OpenAPI path-item and operation parameters). Th
- [2026-09-22] #7240 skipped: Issue #7240 ("Your verification bar is being set now") is a promotional/spam post from the self-described operator of "Agent Embassy," a paid third-party verification service. The body pitches a metered "trust_report" product and asks maintainers to collaborat
- [2026-09-22] #7241 skipped: #7241 is a feature request (Cache-stable progressive disclosure of tools: deferred tools + per-turn selection hook), not a defect. It proposes a large new API surface (DeferredTools, selector hook, search_tools/call_tool built-ins, cache-fingerprint changes, s
- [2026-09-23] #7257 skipped: #7257 is an RFC/promotional proposal (Zero-Scan Project Memory .agent/ spec), not a defect or minimal fix. It asks ADK to adopt an entire third-party specification (new .agent/ directory format, BOOT.md/PROJECT_MAP.json conventions, a new adk.plugins.zeroscan 
- [2026-09-24] #7261 skipped: #7261 is an RFC/feature request ("Would Nano (XNO) as an optional fee-less settlement rail ... be a useful addition?"), not a defect. It proposes integrating a specific third-party PyPI package (feeless402) for cryptocurrency (Nano/XNO) payment settlement into
- [2026-09-24] #7263 skipped: Root cause is entirely upstream in google-genai (python-genai), not in adk-python. ADK's Gemini wrapper passes the user's GenerateContentConfig straight through to google.genai.Client.aio.models.generate_content() with no transformation of service_tier for the
- [2026-09-24] #7273 skipped: #7273 is a feature request (new PgVectorMemoryService integration package under google.adk.integrations.pgvector), not a defect. It requires a brand-new optional dependency extra (psycopg, psycopg-pool, pgvector) and new DB schema/migrations, which is far outs
- [2026-09-25] #7275 skipped: Issue #7275 is a feature proposal (title "Proposal: Security-evaluation plugin for ADK agents via BasePlugin"), not a bug. The reporter explicitly asks three unresolved maintainer design questions (whether ADK wants this feature at all; where it should live --
- [2026-09-25] #7278 skipped: Root cause is entirely upstream in a2a-python (a2a-sdk), not in adk-python, and it is already fixed there.
- [2026-09-25] #7279 skipped: Already fixed on main. Commit f4497801 ("feat: stream partial function-call arguments across model adapters", 2026-09-10) rewrote ApigeeLlm._upsert_tool_call so the is_streaming branch only accumulates raw arguments deltas + a partial-args tracker, then _get_content_parts joins and parses once after the stream completes -- exactly what the issue asks for. The issue was filed against google-adk 2.9.2, a release cut before f4497801 landed, so the report was accurate for that version but stale against current main/upstream. Reproduced the issue's exact fragmented-chunk sequence against ChatCompletionsResponseHandler on this checkout: no crash, correct merged args. Also found a stray self-authored "picking this up" comment on the issue with no branch/PR behind it (same dead-claim pattern as #6960/#7010) -- an earlier run today must have started and died before verifying against current code.
- [2026-09-25] #7279 skipped: Issue #7279 describes ApigeeLlm._upsert_tool_call parsing each streamed
- [2026-09-25] #7288 skipped: RFC/promotional proposal (reporter pitching their own third-party payment product, BlockRun, an OpenAI-compatible host with no API key that settles per-call in USDC over x402), not a defect. Reporter explicitly asks maintainers an open design question -- whether a credential-less host belongs in LLMRegistry as a class with its own pattern, or should stay out of the registry -- and admits uncertainty about whether new backends should be classes at all. Same shape as #7261/#7111/#7257 (third party pitching integration of their own product). Also found a stray self-authored "picking this up" comment with no branch/PR behind it -- same dead-claim pattern as #6960/#7010/#7279.
- [2026-09-25] #7288 skipped: #7288 is an RFC/promotional proposal, not a defect. The reporter is promoting their own third-party payment product (BlockRun, an OpenAI-compatible host that settles per-call in USDC over x402, no API key) and explicitly asks maintainers an open design questio
- [2026-09-25] #7289 skipped: Issue #7289 requires editing GoogleOidcVerifier in src/google/adk/cli/trigger_routes.py, which verifies Google-signed OIDC ID tokens to authenticate incoming Pub/Sub/Eventarc trigger webhook requests -- this is auth/credential code, which the task instructions
- [2026-09-25] #7290 skipped: #7290 asks to implement adk conformance test --mode live, an entirely unimplemented feature (currently a TODO stub returning "Live mode is not implemented yet" in src/google/adk/cli/conformance/cli_test.py). This requires new live-execution and evaluation logi
